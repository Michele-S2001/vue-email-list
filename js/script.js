const { createApp } = Vue;

createApp({
  data() {
    return {
      message: 'hello vue!'
    }
  },

  methods: {
    //funzioni
    fetchEmails() {
      //richiesta emails
    }
  }
}).mount('#app')